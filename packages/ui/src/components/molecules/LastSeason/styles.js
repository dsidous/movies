import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(
  theme => ({
    allBtn: {
      background: 'none',
      border: 'none',
      color: '#666',
      display: 'inline-block',
      fontSize: '1.2em',
      fontWeight: 'bold',
      margin: '20px 10px',
    },
  }),
  { name: 'lastSeason' },
);
